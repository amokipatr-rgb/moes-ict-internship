# To avoid committing machine-specific compiled files
*.pyc
__pycache__/
# To keep diffs clean — regenerated on every test run
.pytest_cache/

# To keep diffs readable — binary DOCX produces unreadable diffs
*.docx
# To avoid tracking single-use utilities — only needed to generate the logbook
generate_logbook.py
setup_conflict.py
