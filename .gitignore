# Machine-specific and compiled artifacts
__pycache__/
*.pyc
.pytest_cache/

# Documents and reports (kept local, not source code)
*.docx
*.pdf

# Images / screenshots (except the network topology deliverable)
*.png

# Sensitive personal data — never commit
Payslip*.pdf

# Single-use logbook generator utilities
generate_logbook*.py
setup_conflict.py
